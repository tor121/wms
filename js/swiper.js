document.addEventListener('DOMContentLoaded', () => {
  const swiper = new Swiper('.swiper-background', {
    loop: true,
    setWrapperSize: true,
    autoplay: {
      delay: 7000,
    },
    speed: 4000,
  });
});
